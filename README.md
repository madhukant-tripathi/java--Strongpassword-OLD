# java--Strongpassword
This Java program checks whether a user-entered password is strong or weak. It checks the password length and the number of uppercase letters, lowercase letters, digits, and special characters. The password is accepted only when all required conditions are satisfied.
